/* ============================================================
   /builds 页面区块 3 Builds Comparison Table 的 Shell 数据源。
   ⚠️ 只做别名重导出，真实 source of truth 放在 data/shells.ts，
   后续改数据直接改 data/shells.ts，不要改这里。
   ============================================================ */

export { SHELLS as SHELLS_FOR_BUILDS_COMPARISON } from "@/data/shells";
export type { Shell as ShellFromJson } from "@/data/shells";
